<div align="center">
  <img src="icons/brand-mark.svg" width="96" alt="شعار HAJB">
  <h1>HAJB — حَجْب</h1>
  <p><strong>خطّك الزمني، بقرارك.</strong></p>
</div>

إضافة متصفح خفيفة تمنحك زرًا مباشرًا لحظر أو كتم حسابات X من داخل التغريدة. تعمل محليًا، بلا حساب، وبلا تتبع أو إرسال بيانات.

## لماذا HAJB؟

- **نقرة واحدة:** اختر الحظر أو الكتم مرة واحدة، ثم نفّذ الإجراء مباشرة من كل تغريدة.
- **غير مهتم:** أخفِ المنشور وامنح توصيات X إشارة مباشرة من زر مستقل بجوار زر الحساب.
- **لوحة تحكم:** شغّل أو أوقف HAJB وأزراره بشكل مستقل، وراجع عدد مرات الحظر والكتم و«غير مهتم» المخزنة محليًا.
- **واجهة عربية وإنجليزية:** تتبع لغة المتصفح وتدعم واجهات X العربية والإنجليزية.
- **استجابة واضحة:** تتزامن الأيقونة والتسمية مع الإجراء المختار، وتُستعاد التغريدة كما كانت إذا فشل التنفيذ.
- **أداء مناسب للتمرير:** مستمع أحداث واحد، تخزين مؤقت للإعدادات، ومعالجة مستهدفة لتغيّرات DOM.
- **خصوصية محلية:** لا توجد خوادم أو تحليلات أو اتصالات خارجية.

## الهوية

HAJB هي كتابة لاتينية لكلمة **حَجْب**. يجمع الشعار بين درع، بوابة على شكل H، وشق قطري يرمز إلى قرار المستخدم بإيقاف الضوضاء. تفاصيل الاستخدام والألوان والنبرة موثقة في [`BRAND.md`](BRAND.md).

## التثبيت اليدوي

نزّل حزمة الإضافة وفك ضغطها، ثم:

### Chrome / Edge

1. افتح `chrome://extensions/` أو `edge://extensions/`.
2. فعّل **وضع المطور**.
3. اختر **تحميل إضافة غير معبأة**.
4. حدّد مجلد HAJB الذي يحتوي `manifest.json`.

### Firefox 140+

1. افتح `about:debugging#/runtime/this-firefox`.
2. اختر **تحميل إضافة مؤقتة**.
3. حدّد `manifest.json` من مجلد HAJB.

## التطوير والتحقق

يتطلب Node.js 22 أو أحدث، ولا توجد اعتمادات تشغيل خارجية:

```bash
npm test
npm run check
```

الاختبارات تغطي حقن الزر، مزامنة الإعدادات، دفعات DOM، منع اختيار إجراءات التراجع، واتجاه الواجهة. يفحص الأمر الثاني JavaScript وManifest والترجمات والموارد المحلية.

## الهيكلة

```text
HAJB/
├── .github/workflows/   # فحص مستمر
├── _locales/            # العربية والإنجليزية
├── content/             # زر HAJB وأتمتة الحظر/الكتم
├── icons/               # الشعار والأيقونات
├── popup/               # واجهة اختيار الإجراء
├── scripts/             # التحقق من حزمة الإضافة
├── test/                # اختبارات Node دون مكتبات خارجية
├── BRAND.md             # نظام الهوية
└── manifest.json        # Manifest V3
```

## الخصوصية

HAJB لا تجمع أو ترسل أي بيانات. الإذن الوحيد غير المرتبط بالموقع هو `storage` لحفظ اختيار الحظر أو الكتم محليًا.

## الترخيص

جميع الحقوق محفوظة لمالك المستودع.
